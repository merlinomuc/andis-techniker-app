{
  "name": "andis-techniker-app",
  "version": "3.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "install:all": "npm install --prefix server --omit=dev && npm install --prefix client --include=dev",
    "build": "npm run install:all && npm run build --prefix client",
    "start": "npm start --prefix server",
    "test": "npm test --prefix server"
  }
}
