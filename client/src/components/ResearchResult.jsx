import {marked} from 'marked'; import {ExternalLink,Search,ShieldAlert,Factory,Globe2,CheckCircle2} from 'lucide-react';
function SourceGroup({title,icon:Icon,sources}){if(!sources?.length)return null;return <div className="sources"><h3><Icon size={18}/>{title}</h3>{sources.map(s=><a key={s.url} href={s.url} target="_blank" rel="noreferrer"><div><b>{s.title}</b><small>{new URL(s.url).hostname}</small></div><ExternalLink/></a>)}</div>}
export default function ResearchResult({research,recognition,onNew}){return <section className="card"><div className="section-head"><div><span className="eyebrow">RECHERCHEERGEBNIS</span><h2>Technische Recherche</h2></div><ShieldAlert/></div>
 <div className="confirmed"><b>{recognition?.manufacturer||research.normalized?.manufacturer||'Technisches Produkt'} {recognition?.productFamily||research.normalized?.productFamily||''}</b><span>{recognition?.partNumber||recognition?.model||research.normalized?.partNumber||research.normalized?.model||''}</span></div>
 {research.searchPhases?.length>0&&<div className="phase-list">{research.searchPhases.map(p=><div key={p.id} className={p.status==='completed'?'done':'muted'}><CheckCircle2/><span><b>{p.label}</b><small>{p.status==='completed'?`${p.sourceCount} Quellen ausgewertet`:'Hersteller noch nicht eindeutig'}</small></span></div>)}</div>}
 <div className="markdown" dangerouslySetInnerHTML={{__html:marked.parse(research.answer)}}/>
 <SourceGroup title="Offizielle Herstellerquellen" icon={Factory} sources={research.officialSources}/><SourceGroup title="Weitere technische Quellen" icon={Globe2} sources={research.additionalSources}/>
 <button className="primary wide" onClick={onNew}><Search/> Neue Suche</button><details className="debug"><summary>Recherche-Diagnose</summary><pre>{JSON.stringify(research.debug,null,2)}</pre></details></section>}
